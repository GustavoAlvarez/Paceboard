import Image from "next/image";

/** Shared vector identity: a continuous cadence with a forward accent. */
export default function PaceIcon() {
  return <Image src="/icon.svg" width={34} height={34} alt="" className="pace-icon" unoptimized />;
}
