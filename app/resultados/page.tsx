import EventWorkspace from "../components/event-workspace";

export const metadata = { title: "Resultados publicados — Paceboard" };

export default function ResultsPage() {
  return <EventWorkspace view="results" />;
}
